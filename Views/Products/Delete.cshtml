@model EC.Models.Product

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Admin/Shared/_AdminLayout.cshtml";
}

<hr />
<h4 class="card-title" style="margin-top: 1em;">@Model.ProductName</h4>
<hr />

<div class="card mb-3 bg-dark text-white">
    <div class="row no-gutters">
        <div class="col-md-4">
            <img src="@Url.Content(Model.ImagePath)" class="img-fluid card-img rounded">
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <table class=" table-responsive table table-dark table-hover card-text">
                    <tbody>
                        <tr>
                            <td>Product Description</td>
                            <td scope="row"> @Model.Description </td>
                        </tr>
                        <tr>
                            <td>Quantity</td>
                            <td scope="row">@Model.Quantity</td>
                        </tr>
                        <tr>
                            <td>Price</td>
                            <td>@Html.DisplayFor(model => model.Price)</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions">
        <input type="submit" value="Delete" class="btn btn-danger white" /> |
        <div class="btn btn-danger white"> @Html.ActionLink("Back to List", "Index")</div>
    </div>
}
   
